# ##############################################################################
# Project Config File
#
# Author Name / Email@email.com
#
# ##############################################################################


# ==============================================================================
# Libraries
#
# Creates a list of packages to install. Uncomment any packages you
# intend to use. config.R will load the list of package when
# run. Failing that, it will attempt to install any missing packages.
# ==============================================================================

packages <- c(

    # Remember, your FIRST package should NOT start with a comma!

    # ---------------- Data Import / Export ----------------
    #,'foreign'
    'RODBC'
    ,'XLConnect'
    # ---------------- Data Processing ---------------------
    #,'data.table'
    #,'devtools'
    ,'dplyr'
    #,'foreign'
    #,'Hmisc'
    #,'knitr'
    #,'stringr'
    #,'testthat'
    ,'XLConnect'
    # ---------------- Graphics ----------------------------
    ,'ggplot2'
    )
if(length(packages[!packages %in% installed.packages()]) > 0 ) {
    install.packages( packages[!packages %in% installed.packages()] )
}
lapply(packages, library, character.only=T)


# ==============================================================================
# Sourced Files
#
# List of function files from lib you want loaded into ALL .R files in
# the root directory. These files can assume all libraries are loaded.
# ==============================================================================

# source("lib/file_name.R")
# source("lib/file_name.R")


# ==============================================================================
# ODBC
# You must uncomment RODBC in config.R to use this section.
# ==============================================================================
con <- odbcConnect('DataMart'
                   ,uid = getOption("DataMart")$uid
                   ,pwd = getOption("DataMart")$pwd
                   )

# .Last <- function(){
#  odbcClose(con)
# }


# ==============================================================================
# Variables
#
# Includes all variables you want declared throughout the project.
#
# This includes variables such as date ranges AND configuration
# tweaks such as setting the width of a tab.
# ==============================================================================


# ---------- knitr ----------
# Markdown -> HTML Style
#library(knitr)
#options(
#    rstudio.markdownToHTML = function(inputFile, outputFile) {
#        require(markdown)
#        markdownToHTML(inputFile, outputFile, stylesheet='HZA.css')
#    }
#)

# This is something I should work towards.
# options(
#     markdown.HTML.header = unlist(
#         sapply(
#             system.file(
#                 "misc"
#                 ,c("HZA.css", "datatables.txt")
#                 ,package = "knitr"
#             )
#             , readLines
#         )
#     )
# )

# ---------- General R Options ----------
# options(prompt="> ")
# options(continue="+ ")
options(tab.width = 4)
options(width = 80)
options(graphics.record=TRUE)


